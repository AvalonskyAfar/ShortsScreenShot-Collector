# 短视频图片数据采集器：操作教程

这份教程按“第一次使用”的顺序写。完成一次配置后，后续只需要启动 Windows 接收端、打开手机悬浮窗并点击开始。

## 采集前准备

请确认：

1. Windows 电脑和 Android 手机连接在同一个可信 Wi-Fi/局域网中，或者准备一根支持数据传输的 USB 数据线。
2. 手机电量充足，建议关闭省电模式，并允许采集器在后台运行。
3. 已将 `ShortVideoCollector-debug.apk` 安装到手机。
4. 已知晓并遵守目标短视频平台条款、版权和隐私要求。

> 不建议使用公共 Wi-Fi。接收端会显示配对码，手机只有填写正确配对码才能上传图片。

## 第一步：启动 Windows 接收端

1. 打开项目中的 `windows` 文件夹。
2. 双击 `start_receiver.bat`。
3. 首次运行 Windows 防火墙可能会询问是否允许网络访问：选择“专用网络”，不要勾选公共网络。
4. 接收端窗口会自动启动，并显示四项信息：

   - **电脑局域网 IP**：例如 `192.168.1.10`
   - **端口**：默认 `8765`
   - **配对码**：首次自动生成的六码数字
   - **数据集目录**：图片最终保存位置

5. 保持这个窗口处于“运行中”，不要关闭。

如果电脑有多个网卡，优先使用与手机同一 Wi-Fi 网段的 IP。通常家庭 Wi-Fi 下是类似 `192.168.x.x` 或 `10.x.x.x` 的地址。

## 第二步：安装 APK

任选一种方式。

### 方式 A：直接安装

把 `release/ShortVideoCollector-debug.apk` 传到 Android 手机上，点开文件安装。

若系统提示“禁止安装未知应用”，按系统提示允许当前文件管理器或浏览器安装即可。

### 方式 B：USB 安装

1. 手机开启“开发者选项”和“USB 调试”。
2. 用 USB 线连接电脑，并在手机上确认调试授权。
3. 在项目根目录打开 PowerShell，执行：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\install_android.ps1
```

成功后，手机桌面会出现“短视频采集器”。

## 第三步：配置手机与电脑连接

采集图片有两种连接方式：无线模式填写 Windows 接收端显示的局域网 IP；USB 数据线模式先运行项目根目录的 `enable_usb_debug.ps1`，手机填写 `127.0.0.1`，不依赖 Wi-Fi。

1. 打开手机上的“短视频采集器”。
2. 在“Windows 接收端”区域填写：

   | 手机字段 | 填写内容 |
   | --- | --- |
   | 电脑地址 | 无线填写 Windows 显示的 IP；USB 填写 `127.0.0.1` |
   | 端口 | 默认是 `8765` |
   | 配对码 | Windows 接收端显示的配对码 |

3. 可先保留默认采集节奏：采样间隔 `900` ms、每视频最多 `12` 帧、最长停留 `15` 秒。
4. 点击“测试连接”。出现“连接成功”即可继续。

如果测试失败，先确认 Windows 接收端正在运行、地址/配对码没有填错；无线模式检查同一局域网，USB 模式检查 `adb devices` 是否显示 `device`，再查看本教程的“故障排查”。

### USB 数据线连接的完整操作

1. 手机开启“开发者选项”和“USB 调试”。
2. USB 线连接电脑，手机确认“允许 USB 调试”。
3. 启动 Windows 接收端。
4. 在项目根目录 PowerShell 执行：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\enable_usb_debug.ps1
```

5. Android 应用地址填写 `127.0.0.1`，端口 `8765`，点击“测试连接”。
6. 采集期间保持数据线连接。结束后执行 `.\disable_usb_debug.ps1` 可移除 USB 转发。

## 第四步：授予 Android 权限

应用主界面按顺序提供了三个按钮，请依次完成。

### 1. 悬浮窗权限

点击“授予悬浮窗权限”，在系统设置中允许“显示在其他应用上层”。

用途：让开始、暂停、停止按钮始终显示在短视频应用上方。

### 2. 无障碍服务

点击“开启自动翻页无障碍服务”，在 Android 的无障碍设置里找到“短视频采集器自动翻页”并开启。

用途：仅用于判断目标应用是否在前台、执行上滑切换视频。它不会读取页面文字、做 OCR 或识别广告。

### 3. 截屏授权

点击“授权截屏并显示悬浮控制器”，在系统提示中允许屏幕录制/截屏。

完成后，手机会显示悬浮控制器，主界面会自动退到后台。Android 会在通知栏显示采集器正在运行，这是正常的前台服务提示。

## 第五步：开始采集

1. 打开目标短视频应用。
2. 手动进入你希望作为第一条的短视频，停留在视频全屏播放页。
3. 点击悬浮窗的“开始”。

采集器会在此时锁定当前前台短视频应用。之后流程是：

```text
保存当前视频画面 → 达到覆盖条件 → 自动上滑 → 确认画面已经变化
→ Windows 创建下一个 video 文件夹 → 继续保存下一条视频画面
```

悬浮窗显示：

- 当前状态；
- Windows 分配的视频编号；
- 当前视频已保存图片数。

悬浮窗顶部可以拖动；点标题可折叠，减少对视频画面的遮挡。建议采集时将它折叠并拖到边缘。

## 暂停、继续与停止

| 操作 | 行为 |
| --- | --- |
| 暂停 | 立即停止后续截图和自动翻页；已保存图片保留。 |
| 继续 | 重新检查目标应用、屏幕和无障碍状态；为防止不同视频混在一起，会新建一个安全的视频目录后恢复。 |
| 停止 | 结束当前采集会话，不会删除任何已保存图片；悬浮窗仍可再次点击开始。 |
| 关闭 | 停止采集并移除悬浮窗；再次使用时回到主界面重新授权截屏。 |

如果你临时要正常使用手机，先点“暂停”。这样采集器不会在你使用其他应用时继续截屏或翻页。

## 采集完成后检查数据

在 Windows 接收端点击“打开数据集”，可以看到：

```text
dataset/
  video_000001/
    frame_000001.png
    frame_000002.png
  video_000002/
    frame_000001.png
```

规则如下：

- 一个 `video_XXXXXX` 文件夹对应一次已确认的视频采集片段；
- `frame_XXXXXX.png` 按该视频内的保存先后编号；
- Windows 接收端重启后会从最大已有视频编号继续；
- 停止、暂停或连接异常不会删除已落盘的图片。

## 采样节奏怎么调

默认值适合先试运行。确认效果后可按目标调整：

| 目的 | 建议 |
| --- | --- |
| 采得更密 | 将采样间隔调到 `500–700` ms，最大帧数调到 `15–25`。 |
| 减少重复图 | 保持 `900–1500` ms，最大帧数 `8–12`。 |
| 让一条视频采得更久 | 增加最长停留秒数，例如 `20–30`。 |
| 短视频切换过快 | 增加最长停留时间和最大帧数。 |

采集器不会无条件保存每一次屏幕刷新：画面变化不足时会跳过重复帧，但每约 3 秒会保留一张保底帧。一个视频的完成条件是“达到最大帧数、最长停留时间，或在最短采集时间后画面持续稳定”。

## 安全暂停的情况

以下情况会自动暂停，而不是继续乱采：

- 切换到目标短视频应用以外的应用；
- 锁屏、熄屏；
- 无障碍服务被关闭；
- Windows 接收端不可连接、配对码错误或磁盘写入失败；
- 上滑后未能在限定时间内确认新视频画面；
- Android 系统结束了截屏授权。

先处理悬浮窗显示的原因，再点击“继续”。如果系统已经结束截屏授权，请回到主界面再次点击“授权截屏并显示悬浮控制器”。

## 常见故障排查

### “测试连接失败”

1. 确认 Windows 接收端窗口状态是“运行中”。
2. 再次核对手机填写的 IP、端口和配对码。
3. 检查手机和电脑是否连在同一个 Wi-Fi，且未使用访客网络/热点隔离网络。
4. 在 Windows 防火墙中允许 Python 的“专用网络”访问，或临时关闭第三方安全软件的网络拦截后重试。
5. 若 IP 发生变化（例如电脑重新连接 Wi-Fi），以接收端窗口的新 IP 为准。

### 悬浮窗没有出现

确认“显示在其他应用上层”已允许；然后回到采集器主界面重新点击“授权截屏并显示悬浮控制器”。

### 无法自动上滑

确认系统无障碍设置中“短视频采集器自动翻页”仍为开启状态。部分厂商系统会在后台清理后自动关闭无障碍服务，需要重新打开。

### 截图是黑屏

部分应用使用 Android 的 DRM 或安全窗口保护。该限制由系统强制，采集器无法绕过。请先在该应用中手动截屏验证；若手动截图也黑屏，该应用画面不能通过此方式采集。

### 自动暂停并提示“未能确认画面已切换”

这表示上滑后画面变化不足，系统为避免把不同视频混入同一目录而暂停。请人工确认视频页面状态后点“继续”；继续会建立新目录，优先保证数据不混组。

### 视频文件夹数比实际视频数多

这是安全策略的正常结果：暂停后继续、连接异常后恢复或切换确认失败后恢复，都会新建目录，避免把用户手动切换后的内容混入旧目录。

## 结束后

1. 在悬浮窗点“停止”。
2. Windows 接收端确认最后一张图片已出现后，点击“停止”或直接关闭窗口。
3. 复制或备份 `dataset` 文件夹即可。后续 OCR、清洗、标注、训练等流程可直接以这些分组图片作为输入。
